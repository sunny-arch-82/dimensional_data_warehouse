governed_analytics:
  target: local
  outputs:
    local:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'postgres') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'analytics') }}"
      pass: "{{ env_var('POSTGRES_PASSWORD', 'analytics_local_only') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'analytics') }}"
      schema: analytics
      threads: 4
